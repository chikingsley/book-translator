# Todo

[ ] get ocr consensus from tesseract, mistral and gemini.
[ ] multiline *italics*
[ ] if user manualy changes the user json, need to be able to seill update after -- right now there's a bug where if the user changes a value, the manu no longer works